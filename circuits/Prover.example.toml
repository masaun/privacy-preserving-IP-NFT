# return = ""
root = ""                  # @dev - Merkle Root
hash_path = ["", ""]
index = ""
secret = ""                # @dev - A hidden metadata hash (private)
expected_nullifier = ""

[ip_nft_data]
nft_owner = ""
nft_token_id = ""
metadata_cid_hash = ""

# root: pub Field, // @dev - Merkle Root
# hash_path: [Field; 2],
# index: Field,
# secret: Field,   // @dev - A hidden metadata hash (private)
# ip_nft_data: IPNftData

# pub struct IPNftData {
#     nft_owner: Field,
#     nft_token_id: Field,
#     metadata_cid_hash: Field  // IPFS CID for the metadata of NFT, which is generated when uploading to IPFS.
# }